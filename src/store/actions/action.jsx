
import axios  from '../../api/axiosConfig';

const runUser = () => async (dispatch, getState) => {
    try {
        const getUser = axios
    } catch (error) {
        console.log(error);
        
        
    }
}